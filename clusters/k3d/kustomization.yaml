apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# This is the sync target of bootstrap/root/application.yaml. Add platform
# Applications and cluster-scoped resources here as they come online.
resources:
  - apps-applicationset.yaml
